# Development requirements — covers everything (mirrors "ai-houkai[dev]" + tui).
# For end-user installs prefer the extras: pip install "ai-houkai[cli|tui|all]".

# core
chromadb>=0.5.0
sentence-transformers>=2.7.0
# tomllib backport: stdlib from 3.11, so only 3.10 needs the wheel.
tomli>=2.0.1; python_version < "3.11"

# MCP server
# <2: mcp 2.0.0 removed mcp.server.fastmcp (see pyproject for the full note).
mcp>=1.2.0,<2

# Claude agent example + anthropic: summarizer
anthropic>=0.40.0

# OpenAI agent example + openai: summarizer
# Ollama agent example (uses the same openai client via Ollama's OAI-compat endpoint)
openai>=1.50.0

# houkai CLI
typer>=0.12
rich>=13.7

# houkai tui memory browser
textual>=0.50

# tests
pytest>=8.0.0
pytest-asyncio>=0.24
